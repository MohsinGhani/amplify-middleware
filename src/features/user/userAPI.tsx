import { SigninPayload, SignupPayload } from "@/shared/interfaces/interfaces";

// src/features/user/userAPI.js
export const userSignup = async (userData: SignupPayload) => {
  // API call to register a new user
  // Replace with your actual API call
};

export const userSignin = async (credentials: SigninPayload) => {
  // API call for user login
  // Replace with your actual API call
};
